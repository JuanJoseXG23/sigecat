import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { Edit3 } from 'lucide-react'
import { useState } from 'react'
import { Button } from '@/components/ui/button'
import { Card } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { Select } from '@/components/ui/select'
import { listUsers, saveUserProfile, setUserActive } from '@/services/user-profile.service'
import { USER_ROLES, type UserProfile } from '@/types/user'

export function UsersPage() {
  const client = useQueryClient(); const { data = [] } = useQuery({ queryKey: ['users'], queryFn: listUsers }); const [search, setSearch] = useState(''); const [role, setRole] = useState(''); const [active, setActive] = useState(''); const [editing, setEditing] = useState<UserProfile | null>(null)
  const refresh = () => client.invalidateQueries({ queryKey: ['users'] }); const toggle = useMutation({ mutationFn: ({ uid, value }: { uid: string; value: boolean }) => setUserActive(uid, value), onSuccess: refresh }); const save = useMutation({ mutationFn: saveUserProfile, onSuccess: () => { void refresh(); setEditing(null) } })
  const rows = data.filter((item) => (!search || `${item.nombreCompleto} ${item.correo}`.toLowerCase().includes(search.toLowerCase())) && (!role || item.rol === role) && (!active || String(item.activo) === active))
  return <section className="mx-auto max-w-7xl space-y-6"><div><p className="text-sm font-medium text-primary">Administración</p><h1 className="text-2xl font-semibold">Usuarios</h1></div><Card className="p-4"><div className="grid gap-3 md:grid-cols-3"><Input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Buscar nombre o correo"/><Select value={role} onChange={(event) => setRole(event.target.value)}><option value="">Todos los roles</option>{USER_ROLES.map((value) => <option key={value}>{value}</option>)}</Select><Select value={active} onChange={(event) => setActive(event.target.value)}><option value="">Todos los estados</option><option value="true">Activos</option><option value="false">Inactivos</option></Select></div></Card><Card className="overflow-x-auto"><table className="w-full min-w-[850px] text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-4">Usuario</th><th>Rol</th><th>Cargo</th><th>Dependencia</th><th>Último acceso</th><th>Estado</th><th/></tr></thead><tbody>{rows.map((item) => <tr className="border-t" key={item.uid}><td className="p-4"><b>{item.nombreCompleto}</b><small className="block text-slate-500">{item.correo}</small></td><td>{item.rol}</td><td>{item.cargo}</td><td>{item.dependencia ?? '—'}</td><td>{item.ultimoIngreso?.toDate().toLocaleString('es-CO') ?? 'Sin acceso'}</td><td>{item.activo ? 'Activo' : 'Inactivo'}</td><td className="space-x-1"><Button variant="ghost" size="sm" aria-label="Editar usuario" onClick={() => setEditing(item)}><Edit3 size={16}/></Button><Button variant="outline" size="sm" onClick={() => toggle.mutate({ uid: item.uid, value: !item.activo })}>{item.activo ? 'Desactivar' : 'Activar'}</Button></td></tr>)}</tbody></table></Card>{editing && <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><Card className="w-full max-w-xl p-6"><h2 className="text-lg font-semibold">Editar usuario</h2><div className="mt-4 grid gap-3 sm:grid-cols-2"><Input value={editing.nombreCompleto} onChange={(event) => setEditing({ ...editing, nombreCompleto: event.target.value })} placeholder="Nombres y apellidos"/><Input value={editing.correo} onChange={(event) => setEditing({ ...editing, correo: event.target.value })} placeholder="Correo institucional"/><Input value={editing.cargo} onChange={(event) => setEditing({ ...editing, cargo: event.target.value })} placeholder="Cargo"/><Input value={editing.dependencia ?? ''} onChange={(event) => setEditing({ ...editing, dependencia: event.target.value })} placeholder="Dependencia"/><Select value={editing.rol} onChange={(event) => setEditing({ ...editing, rol: event.target.value as UserProfile['rol'] })}>{USER_ROLES.map((value) => <option key={value}>{value}</option>)}</Select></div><div className="mt-5 flex justify-end gap-2"><Button variant="outline" onClick={() => setEditing(null)}>Cancelar</Button><Button disabled={!editing.nombreCompleto.trim() || !editing.correo.trim() || save.isPending} onClick={() => save.mutate(editing)}>Guardar cambios</Button></div></Card></div>}</section>
}
