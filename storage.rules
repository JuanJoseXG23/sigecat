rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Firebase Storage no se usa aún: negar todo hasta implementar controles.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
